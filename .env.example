# Database — 使用你创建的 mylifeplan 用户，不要用 postgres
# 密码含 @ 等特殊字符时需 URL 编码，例如 @ → %40
DATABASE_URL="postgresql://mylifeplan:GC%40z6zMAY9Ni@localhost:5432/mylifeplan?schema=public"

# Auth — 至少 32 位随机字符串（openssl rand -base64 32）
AUTH_SECRET="your-generated-secret-here"

# 生产环境
NODE_ENV="production"
